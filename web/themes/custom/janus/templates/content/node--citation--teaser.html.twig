<span {{ attributes }}>
  {{ content.field_authors }}
    <strong{{ title_attributes }}>
      {% if citation_link %}
        <a href="{{ citation_link }}" target="_blank"><span>"{{ label }}",</span></a>
      {% else %}
        <span>"{{ label }}",</span>
      {% endif %}
    </strong>
  {{ content.field_publication_information }}
  {{ title_suffix }}
</span>
